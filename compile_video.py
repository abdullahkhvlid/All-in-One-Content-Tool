from utils.logger import log_event

def compile_video(video_path, audio_path, output_path):
    """
    Placeholder function. No compilation is performed.
    """
    log_event("INFO", "compile_video called (placeholder, no-op).", "compile_video")
    return None

if __name__ == "__main__":
    # No-op run
    log_event("INFO", "compile_video module executed directly (no-op).", "compile_video")
